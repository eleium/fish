# 真正的关机签到方案 - 云端部署

由于本地脚本需要电脑开机才能运行，如果你想要在**关机的情况下也能每天自动签到**，建议使用云端部署方案。

## 方案一：使用 GitHub Actions（免费推荐）

### 步骤：

1. **创建 GitHub 仓库**
   - 访问 https://github.com
   - 创建一个新的公开或私有仓库
   - 将 `auto_sign.py` 上传到仓库

2. **创建工作流文件**
   
   在仓库中创建 `.github/workflows/sign.yml` 文件：

```yaml
name: FishC Daily Sign

on:
  schedule:
    # 每天 UTC 时间 0:00 运行（北京时间 8:00）
    - cron: '0 0 * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  sign:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Run sign script
      env:
        FISHC_USERNAME: ${{ secrets.FISHC_USERNAME }}
        FISHC_PASSWORD: ${{ secrets.FISHC_PASSWORD }}
      run: python auto_sign_github.py
```

3. **配置环境变量（账号密码）**
   - 在仓库页面点击 Settings
   - 左侧菜单选择 Secrets and variables → Actions
   - 点击 New repository secret
   - 添加两个 secrets：
     - `FISHC_USERNAME`: 你的鱼 C 论坛用户名
     - `FISHC_PASSWORD`: 你的鱼 C 论坛密码

4. **修改代码支持环境变量**
   
   创建 `auto_sign_github.py` 文件（见下方代码）

5. **提交并测试**
   - 提交所有文件
   - 在 Actions 标签页手动运行一次测试
   - 之后每天会自动运行

### 优点：
- ✅ 完全免费
- ✅ 真正 24 小时运行，不依赖本地电脑
- ✅ 可以查看每次运行日志
- ✅ 稳定可靠

---

## 方案二：使用云服务器（VPS）

### 推荐服务商：
- 阿里云 ECS（有免费试用）
- 腾讯云 CVM（有免费套餐）
- AWS EC2（12 个月免费层）
- Google Cloud（有免费额度）

### 部署步骤：

1. **购买/申请云服务器**
   - 选择最低配置即可（1 核 1G 足够）
   - 系统选择 Ubuntu 或 CentOS

2. **连接服务器**
   ```bash
   ssh root@your_server_ip
   ```

3. **安装 Python**
   ```bash
   sudo apt update
   sudo apt install python3 python3-pip -y
   ```

4. **上传脚本**
   ```bash
   # 使用 scp 上传
   scp auto_sign.py root@your_server_ip:/root/
   scp requirements.txt root@your_server_ip:/root/
   ```

5. **安装依赖**
   ```bash
   cd /root
   pip3 install -r requirements.txt
   ```

6. **设置定时任务**
   ```bash
   crontab -e
   ```
   
   添加一行（每天北京时间 8:00 执行）：
   ```
   0 0 * * * /usr/bin/python3 /root/auto_sign.py >> /root/sign.log 2>&1
   ```

7. **首次运行登录**
   ```bash
   python3 auto_sign.py
   # 输入账号密码完成首次登录
   ```

### 优点：
- ✅ 专业稳定
- ✅ 可控制性强
- ✅ 适合长期运行

### 缺点：
- 💰 需要一定费用（除非使用免费套餐）

---

## 方案三：使用在线自动化平台

### 推荐平台：
- **腾讯云云函数 SCF**（有免费额度）
- **阿里云函数计算 FC**
- **AWS Lambda**

这些平台提供 Serverless 服务，按执行次数计费，有免费额度。

---

## 总结建议

| 方案 | 成本 | 难度 | 稳定性 | 推荐度 |
|------|------|------|--------|--------|
| GitHub Actions | 免费 | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 云服务器 | 付费 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 本地脚本 | 免费 | ⭐ | ⭐⭐ | ⭐⭐ |
| 云函数 | 基本免费 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

**强烈推荐使用 GitHub Actions 方案**，完全免费且不需要自己的服务器，真正实现关机也能自动签到！
